name: Deploy to Hostinger

on:
  push:
    branches:
      - main

jobs:
  build-and-deploy:
    runs-on: ubuntu-latest

    steps:
    - name: Checkout Code
      uses: actions/checkout@v3

    - name: Set Up Node.js
      uses: actions/setup-node@v3
      with:
        node-version: '16'

    - name: Install Dependencies
      run: |
        cd frontend
        npm install

    - name: Build the Project
      run: |
        cd frontend
        npm run build

    - name: Deploy to Hostinger
      env:
        HOST: ${{ secrets.HOSTINGER_HOST }}
        USERNAME: ${{ secrets.HOSTINGER_USERNAME }}
      run: |
        # Create an SSH key file
        echo "${{ secrets.HOSTINGER_SSH_KEY }}" > private_key
        chmod 600 private_key

        # Deploy build contents from the frontend/build directory
        rsync -avz -e "ssh -p 65002 -i private_key -o StrictHostKeyChecking=no" \
          --delete frontend/build/ $USERNAME@$HOST:domains/hackfd-rangeready.ca/public_html/